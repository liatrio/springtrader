apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "springtrader-chart.fullname" . }}-test-connection"
  labels:
{{ include "springtrader-chart.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "springtrader-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
